/*
fun howManyPlayers(): Int {
    var numPlayers: Int
    do {
        println(chosenLanguage?.getMessage("howmanypalyers"))
        print("${GREEN.ON}--> ${GREEN.OFF}")
        val input = readLine()
        numPlayers = input?.toIntOrNull() ?: -1
        if (numPlayers <= 0) {
            println("Number of players must be greater than 0.")
        }
    } while (numPlayers <= 0)
    return numPlayers
}*/
